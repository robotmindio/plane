Plane has been installed.

{{- if .Values.ingress.enabled }}

Access Plane at:
{{- if .Values.ingress.tls.enabled }}
  https://{{ .Values.ingress.appHost }}
{{- else }}
  http://{{ .Values.ingress.appHost }}
{{- end }}

Admin panel (god-mode):
{{- if .Values.ingress.tls.enabled }}
  https://{{ .Values.ingress.appHost }}/god-mode/
{{- else }}
  http://{{ .Values.ingress.appHost }}/god-mode/
{{- end }}
{{- else }}

No ingress is configured. Forward the web service port to access Plane:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "plane.fullname" . }}-web 3000:3000
  open http://localhost:3000
{{- end }}

Next steps:
  1. Open the URL above and create your admin account
  2. Configure email (SMTP) and OAuth in the admin panel

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
